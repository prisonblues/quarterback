# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
*.local

# Per-deployment compose overlay. Auto-loaded by `docker compose up`, and it carries
# that deployment's secrets and auth posture — it is site config, not part of the app.
docker-compose.override.yml

# Local data
logs/
pgdata/
