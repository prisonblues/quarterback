# trim the root README

The root README is now a concise project entry point instead of a combined manual and release
history, and the release job no longer writes release-history bullets back into it.
